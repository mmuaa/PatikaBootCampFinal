<form method="post" action="{{ path('app_admin_message_delete', {'id': read.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ read.id) }}">
    <button class="btn btn-block btn-danger">Delete</button>
</form>
