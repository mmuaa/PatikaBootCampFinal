{% extends 'adminbase.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Mesajlar</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="{{ path('app_admin') }}">Home</a></li>
                        <li class="breadcrumb-item active">Mesajlar</li>
                    </ol>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">

        <!-- Default box -->
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Yeni Mesajlar</h3>

                <div class="card-tools">
                    <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                        <i class="fas fa-minus"></i>
                    </button>
                    <button type="button" class="btn btn-tool" data-card-widget="remove" title="Remove">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
            </div>
            <div class="card-body">

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Subject</th>
                <th>Message</th>
                <th>Status</th>
                <th>Ip</th>
                <th>Created_at</th>
                <th>Updated_at</th>
                <th></th>
                <th>actions</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for message in messages %}
            <tr>
                <td>{{ message.id }}</td>
                <td>{{ message.name }}</td>
                <td>{{ message.subject }}</td>
                <td>{{ message.message }}</td>
                <td>{{ message.status }}</td>
                <td>{{ message.ip }}</td>
                <td>{{ message.createdAt ? message.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ message.updatedAt ? message.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a class="btn btn-block btn-info" href="{{ path('app_admin_message_show', {'id': message.id}) }}">show</a>
                </td>
                <td>
                    <a class="btn btn-block btn-warning" href="{{ path('app_admin_message_edit', {'id': message.id}) }}">edit</a>
                </td>
                <td>
                    {{ include('admin/message/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
            </div>
            <!-- /.card-body -->
            <div class="card-footer">
                Footer
            </div>
            <!-- /.card-footer-->
        </div>
        <!-- /.card -->
        <!-- Default box -->
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Okunmuş Mesajlar</h3>

                <div class="card-tools">
                    <button type="button" class="btn btn-tool" data-card-widget="collapse" title="Collapse">
                        <i class="fas fa-minus"></i>
                    </button>
                    <button type="button" class="btn btn-tool" data-card-widget="remove" title="Remove">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
            </div>
            <div class="card-body">
                <table class="table">
                    <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Subject</th>
                        <th>Message</th>
                        <th>Status</th>
                        <th>Ip</th>
                        <th>Created_at</th>
                        <th>Updated_at</th>
                        <th></th>
                        <th>actions</th>
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for read in messagesread %}
                        <tr>
                            <td>{{ read.id }}</td>
                            <td>{{ read.name }}</td>
                            <td>{{ read.subject }}</td>
                            <td>{{ read.message }}</td>
                            <td>{{ read.status }}</td>
                            <td>{{ read.ip }}</td>
                            <td>{{ read.createdAt ? read.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                            <td>{{ read.updatedAt ? read.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                            <td>
                                <a class="btn btn-block btn-info" href="{{ path('app_admin_message_show', {'id': read.id}) }}">show</a>
                            </td>
                            <td>
                                <a class="btn btn-block btn-warning" href="{{ path('app_admin_message_edit', {'id': read.id}) }}">edit</a>
                            </td>
                            <td>
                                {{ include('admin/message/_delete2_form.html.twig') }}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="9">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <!-- /.card-body -->
            <div class="card-footer">
                Footer
            </div>
            <!-- /.card-footer-->
        </div>
        <!-- /.card -->

    </section>
    <!-- /.content -->
</div>
    <!-- /.content-wrapper -->
{% endblock %}
